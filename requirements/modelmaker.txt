# Dedicated environment for the EfficientDet Model Maker backend.
# Keep this separate from the KerasCV detector environment.
typer>=0.12
rich>=13.7
PyYAML>=6.0
Pillow>=10.0
tqdm>=4.66

# Model Maker compatibility stack (WSL/Linux).
numpy>=1.22,<1.24
tensorflow==2.13.1
tflite-model-maker==0.4.3
