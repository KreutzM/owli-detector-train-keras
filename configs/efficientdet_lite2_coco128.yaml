model:
  variant: lite2

data:
  csv: work/datasets/coco128/modelmaker.csv
  images_dir: data/coco128/images
  label_map_json: work/datasets/coco128/class_names.json

train:
  epochs: 20
  batch_size: 8
  train_whole_model: false

outputs:
  work_dir: work
  out_dir: outputs
